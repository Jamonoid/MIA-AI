# ══════════════════════════════════════════════
# MIA-AI .gitignore
# ══════════════════════════════════════════════

# ── Python ──
__pycache__/
*.py[codz]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
.eggs/

# ── Entorno virtual ──
.venv/
venv/
env/

# ── Modelos ML (pesados, no versionados) ──
models/**/*.gguf
models/**/*.bin
models/**/*.pt
models/**/*.pth
models/**/*.safetensors
models/**/*.onnx
models/**/*.ct2/

# ── Voces (archivos de audio, no versionados) ──
voices/*.wav
voices/*.mp3
voices/*.flac
voices/*.ogg

# ── Datos persistentes ──
data/chroma_db/

# ── Cache de modelos (HuggingFace, TTS, etc.) ──
.cache/
*.onnx_data

# ── Secrets / API keys ──
.env
.envrc
*.secret
.vts_token
config.yaml
config.local.yaml

# ── Logs ──
*.log
logs/

# ── Tests y cobertura ──
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# ── Type checkers ──
.mypy_cache/
.pytype/
.pyre/
.ruff_cache/

# ── IDEs ──
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── uv / pip ──
uv.lock
pip-log.txt

# ── Gemini / AI agents ──
.gemini/
.agent/

# ── Guías privadas (contienen secrets) ──
discord_bot_guide.md
prompts/
config.yaml
